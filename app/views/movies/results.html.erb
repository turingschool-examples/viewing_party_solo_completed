<h1>Movie Results </h1>
<%= button_to "Return to Discover Page", "/users/#{params[:user_id]}/discover", { turbo: false, method: :get } %>

<ul>
   <% @movies.each do |movie| %>
      <li><%= movie.title %>: <%= movie.vote_average %></li>
   <% end %>